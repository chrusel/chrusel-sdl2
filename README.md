# Install on Ubuntu
    sudo apt update
    sudo apt install cmake libsdl2-dev g++

# Clone Repo
    git clone https://github.com/chrusel/chrusel-sdl2.git

# Build
    cd chrusel-sdl2
    mkdir build
    cd build
    cmake ..
    make

# Run
    ./sdl2test

